{
  "suite": "L0:naturalcontrol.actuation_receipt.v1:shape",
  "cases": [
    {
      "name": "L0:naturalcontrol.actuation_receipt.v1:shape-ok",
      "given": "samples/naturalcontrol.actuation_receipt.v1/minimal.json",
      "assert_jsonpath": [
        "$.schema_id == 'naturalcontrol.actuation_receipt.v1'",
        "$.spec_version == '1.0'",
        "$.surface_kind == 'mouse'",
        "$.receipt_state == 'emitted'",
        "$.emitted_atom_count == 3"
      ]
    }
  ]
}
