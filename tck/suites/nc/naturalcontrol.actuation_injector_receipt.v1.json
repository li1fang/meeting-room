{
  "suite": "L0:naturalcontrol.actuation_injector_receipt.v1:shape",
  "cases": [
    {
      "name": "L0:naturalcontrol.actuation_injector_receipt.v1:shape-ok",
      "given": "samples/naturalcontrol.actuation_injector_receipt.v1/minimal.json",
      "assert_jsonpath": [
        "$.schema_id == 'naturalcontrol.actuation_injector_receipt.v1'",
        "$.spec_version == '1.0'",
        "$.backend_kind == 'uinput'",
        "$.receipt_state == 'injected'",
        "$.packet_count == 10"
      ]
    }
  ]
}
